/* eslint-disable no-undef */
module.exports = {
    name: 'help',
    description: 'FORMAT AS: ```!help```\nPROVIDES INFORMATION ON AVAILABLE COMMANDS.',
    execute(message, input) {
        message.channel.send("**Analyzer 1.50, a Leiji-themed Discord bot.**\n\n**Prefix Commands:**\n```!all series```Provides a list of all series in Analyzer's database\n\n```!calculate [QUERY]```Calculates the probability of a given scenario\n\n```!character-check is [CHARACTER] in [SERIES]```Confirms whether or not a recycled Leiji character appears in a series\n\n```!characters in [SERIES]```Provides list of characters that appear in specified series (limited to recurring characters)\n\n```!content-warning [SERIES NAME]```Provides content warnings for specified series\n\n```!file [CHARACTER]```Posts a random photo of requested character. Write [list] instead of a character to see list of characters in database.\n\n```!help```Will make this message appear\n\n```!meme```Will post a Leiji-themed meme\n\n```!op [SERIES]```Posts a link to the OP of the specified series\n\n**Mention Commands:**\n```@Analyzer about```Analyzer will tell you about himself\n\n```@Analyzer calculate```Calculates the probability of a given scenario\n\n```@Analyzer cursed image alert```Posts a request for deletion of the previous post (as a joke)\n\nAnalyzer will also respond to certain compliments, thank yous, and easter egg phrases")
    }
}